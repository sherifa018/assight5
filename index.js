
    function add(a, b) {
      return a + b;
    }
    let add = (a, b) => a + b;

    function multiply(a, b) {
      return a * b;
    }
    let multiply = (a, b) => a * b;

    function greet(name) {
      return "hello," + name;
    }
    let greet = (name) => "hello," + name;
  